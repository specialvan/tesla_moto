# Git 状态评审记录

评审日期：2026-05-15

## 1. 当前评审线

```text
codex-review-line...origin/codex-review-line [ahead 1]
HEAD: 4e0c489 feat: 增加PyFluent高保真工作流入口
```

当前工作区 `G:\tesla_moto-codex` 仍有 8 个未提交 summary 修改：

```text
experiments/exp_001_linear_dq/summary.json
experiments/exp_004_safety_boundaries/summary.json
experiments/exp_005_modulation_factor/summary.json
experiments/exp_006_nonlinear_flux_lut/summary.json
experiments/exp_007_hybrid_excitation/summary.json
experiments/exp_008_winding_reconfiguration/summary.json
experiments/exp_009_multiphase_phase_group/summary.json
experiments/exp_010_weighted_efficiency_pareto/summary.json
```

这些差异不是模型结果变化，而是 `csv_path` / `lut_path` 从 `G:\tesla_moto\...` 改成 `G:\tesla_moto-codex\...` 的绝对路径变化。它说明当前实验 runner 会把本地工作区绝对路径写入跟踪产物，导致同一实验在不同 worktree 中产生噪声 diff。

## 2. Claude 主线

```text
claude-mainline...origin/claude-mainline
HEAD: 391ce20 feat(exp011): 新增铁损模型和Bertotti三相铁损速度扫描实验
```

对照工作树 `G:\tesla_moto` 当前状态：

```text
 M models/control_lut.json
?? .claude/
```

`models/control_lut.json` 的修改只有 `generated_at` 时间戳变化。这与 `tests/test_control_lut_generator.py` 调用默认 `run()` 并写入跟踪文件一致，属于测试污染工作树的证据。

`.claude/` 是本地 agent 工作目录，当前未跟踪。若没有明确长期化需求，应加入忽略规则或在评审/grep 时排除，避免把临时 worktree、缓存和本地设置混入主线事实源。

## 3. 分支差异摘要

`codex-review-line..claude-mainline` 的关键差异：

| 类别 | Claude 主线新增或重构 |
|---|---|
| 文档工程包 | `claude-docs/`、`codex-review/docs/`、`claude-review/README.md` |
| 控制 LUT | `models/control_lut_schema.json`、`models/control_lut.json`、`sim/run_control_lut_generator.py` |
| 非线性 LUT phase-2 | `models/flux_lut_schema.json`、`sim/run_nonlinear_flux_lut_search_experiment.py`、`tests/test_nonlinear_flux_lut_search.py` |
| 铁损 EXP-011 | `sim/iron_loss.py`、`sim/run_iron_loss_experiment.py`、`experiments/exp_011_iron_loss/*`、`tests/test_iron_loss.py` |
| 旧分支清理 | 删除 `sim/control_search.py`、`experiments/exp_001_linear_dq/run.py`、`codex-reviwe/*` |
| 当前评审线独有 | `sim/pyfluent_workflow.py`、`sim/run_pyfluent_workflow.py`、对应 PyFluent 测试 |

## 4. 评审判断

当前不能把 `codex-review-line` 和 `claude-mainline` 简单视作同一状态。Claude 主线已经推进到控制 LUT、EXP-011 铁损和文档工程包；当前评审线新增了 PyFluent 工作流，但缺少主线最新代码。后续需要显式选择：

1. 将本评审包和 PyFluent 工作迁移到 Claude 主线；
2. 将 Claude 主线变更合回当前评审线；
3. 继续分支隔离，但用报告明确各自职责和事实边界。
